import Input from "./Input";

export default function SearchProduct(){
    return(
        <input 
        placeholder="Məhsul axtar..."
         className="
       w-full flex items-center justify-between bg-gray-50 border border-gray-200 rounded-[10px] px-4 py-2 cursor-pointer hover:bg-gray-50 transition-all duration-200 focus:outline-none focus:ring-opacity-20
      "/>
    )
}